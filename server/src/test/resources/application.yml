spring:
  datasource:
    url: jdbc:h2:mem:addressbook_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
jsonserver:
  base-url: http://localhost:3000/contacts
